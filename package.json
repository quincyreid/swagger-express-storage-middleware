{
  "name": "swagger-express-storage-middleware",
  "version": "1.0.0",
  "description": "Storage middleware for use alongside swagger-express-middleware.",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha",
    "build": "rimraf lib && babel src --out-dir lib --source-maps",
    "watch": "npm run build -- --watch",
    "prepublish": "npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "babel": "^5.1.13",
    "debug": "^2.1.3",
    "lodash": "^3.7.0",
    "rimraf": "^2.3.2",
    "waterline": "^0.10.21"
  },
  "peerDependencies": {
    "express": "4.x",
    "swagger-express-middleware": "*"
  },
  "devDependencies": {
    "mocha": "^2.2.4"
  },
  "author": "attn:",
  "repository": {
    "type": "git",
    "url": "https://github.com/attn/swagger-express-storage-middleware.git"
  },
  "bugs": {
    "url": "https://github.com/attn/swagger-express-storage-middleware/issues"
  },
  "homepage": "https://github.com/attn/swagger-express-storage-middleware",
  "keywords": [
    "datastore",
    "express",
    "middleware",
    "orm",
    "storage",
    "swagger",
    "waterline"
  ]
}
